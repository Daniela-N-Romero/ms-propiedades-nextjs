export const styles = {
  // Contenedor Flotante Premium con efecto Glassmorphism
  searchContainer: "w-full bg-white/95 backdrop-blur-md p-4 sm:p-6 rounded-2xl shadow-2xl border border-slate-200 text-left max-w-5xl mx-auto",
  
  // Labels limpias y bien separadas
  label: "block text-[11px] font-extrabold text-slate-700 uppercase tracking-wider mb-1.5",
  
  // Selects estilizados sin hacks de bordes transparentes
  select: "w-full bg-slate-900 text-white text-xs font-medium rounded-xl border border-slate-700 p-3 focus:outline-none focus:ring-2 focus:ring-brand-cyan transition-all cursor-pointer appearance-none truncate pr-8",
  
  // Wrapper para meter la flechita personalizada a la derecha
  selectWrapper: "relative w-full",
  
  // Botón Buscar con presencia comercial
  searchBtn: "w-full bg-brand-green hover:bg-green-600 text-white font-extrabold py-3 px-6 rounded-xl text-xs sm:text-sm transition-all shadow-md hover:shadow-lg uppercase tracking-wider font-spartan flex items-center justify-center gap-2 cursor-pointer h-[42px]",
};